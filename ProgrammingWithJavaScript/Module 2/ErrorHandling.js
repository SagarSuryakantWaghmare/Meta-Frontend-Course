function add(a,b){
    return a+b;
}
// When we concate the string and the num then we get output as the String
console.log(typeof(add('4',2)))
// Refernece error
// Syntax error
// Type error

// Try catch blocks
